name: Validate TALVO ReceiveSupplyItem on staging

on:
  workflow_dispatch:
    inputs:
      confirmation:
        description: "Type VALIDATE_TALVO_RECEIVE_STAGING"
        required: true
        type: string
      target_sha:
        description: "Exact feature commit SHA; must equal talvo-staging APPROVED_TALVO_TARGET