name: Validate PR 40 atomic operations on staging

on:
  workflow_dispatch:
    inputs:
      confirmation:
        description: "Type VALIDATE_PR40_STAGING to confirm staging-only validation"
        required: true
        type: string

permissions:
  contents: read

jobs:
  validate-pr40:
    uses: ./.github/workflows/pr32-staging-atomic-validation.yml
